pipeline {
    agent any
    stages {
        stage("hello") {
            steps {
                sh "pwd"
                sh "ls -al"
                sh "go version"
            }
        }
    }
}
